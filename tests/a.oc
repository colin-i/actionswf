
format elfobj64

importx "_exit" exit

einclude "../dev/import.h"

entryraw _start(sd *argc,sd *argv0,ss *argv1)

call swf_new_ex("a.swf",0x80,0x80,0x001100,2,(debug_log|debug_x))

call action("
getVersion();
")

call swf_done()

call exit(0)
