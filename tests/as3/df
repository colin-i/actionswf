f=../.expected/as3
if [ "${platform}" = win ]; then
	cd dest
		find . -type f -exec dos2unix {} \;
	cd ..
fi
diff ${f} dest || {
	if [ -z "${interact}" ]; then
		exit 1
	fi
	read;
	cp dest/Main.hx ${f}
	cp dest/MyClip.hx ${f}
}
