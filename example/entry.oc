
entryraw main(sd argc,sd *argv0,ss *argv1)
sd flags=flag_pool|debug_x_pad|debug_log
if argc!=1
	or flags (flag_pool_read)
	or flags (flag_x) #for tests/data
	or flags (debug_titles) #on display for oadbg
end
#sub argv1# 0x30
