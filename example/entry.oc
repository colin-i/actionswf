
entryraw main(sd argc,sd *argv0,ss *argv1)
sd flags=flagpre_log|flagpre_pool_write|debug_xpad
if argc!=1
	or flags (flag_pool_uread|flag_x) #for tests/data
	or flags (flag_titles)
end
#sub argv1# 0x30
