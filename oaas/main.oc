
format elfobj64

^import "spc" spc 1

functionx line(ss oa)
	#remove "text" 'text'
	#a . af . function a() -> function a(e) , find ()
	#r . r_x _x -> x
	#x next is untranslated
	set oa spc(oa)
	return oa
end
